#include <stdio.h>
int main(){
    int width = 12;
    int height =13;
    int area;
    area = width * height; //calculation with width and height
    printf("The width is: %d\n", width);
    printf("The Height is: %d\n", height);
    printf("The area is: %d", area);
    return 0;
}